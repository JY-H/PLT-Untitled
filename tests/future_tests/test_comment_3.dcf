/* Tests comments within lines with statements. */

class TestComment {
	main() -> int {
		int a  = 0;	// This should work
		int b = 0;	/* This should also work */
		int c = /* This should still work */ 0;
		return 0;
	}
}
